# CLAUDE.md

> Claude Codeがリポジトリを扱う際に最初に読むファイル。
> 「何をするか」より「何をしないか」を明確に書くことが精度向上の核心。

---

## 1. プロジェクト概要

### 何を作っているか
昨今のAIロボティクスの進化によって可能となるのは自身の人格の複数化である。
そこでAIのメモリ記憶・及び言語理解能力から自分自身のコピーのようなものを
作成し、そして一か所にいなければいけないというその不便さを解決したい。
- 形態　　：　始めは、Webアプリケーション
- 利用者　：　不特定多数（はじめは、少ないコミュニティで初めて同心円状に広げる。）
- 中核機能：　MBTIやLLMに対する自己紹介によって人格のひな型を作成しユーザー・LLM同士の対話で人格を決定していく。

### 技術スタック
| 層 | 技術 |
|---|---|
| フロントエンド | vite |
| バックエンド | VercelのAPI Routes |
| DB | supabase |
| インフラ | Vercel |

### 現在のフェーズ
<!-- MVP開発中 / 本番運用中 / リファクタリング中 など -->
MVP開発中、及びアーキテクチャの全体設計のフェーズ
---

## 2. 実行コマンド

```bash
# 開発サーバー
npm run dev
# テスト
npm test

# Lint / フォーマット
npm run lint
npm run format
```

### ⚠️ 実行禁止
```bash
# ローカルで手動buildの禁止
npm run build   # 例: 不要なアーティファクトが生成される
```
---

## 3. アーキテクチャ

### ディレクトリ構成
```
src/
├── components/
├── features/
├── lib/
│   ├── supabase.ts   # Supabaseクライアント
│   └── services/     # ビジネスロジックはここに置く
└── utils/

api/                  # Vercelが自動でエンドポイント化する（ここがバックエンド）
├── personas/
│   └── create.ts     # POST /api/personas/create
└── chat/
    └── send.ts
```

### 責務の境界
| 層 | 持ってはいけないもの |
|---|---|
| components/ | APIコール・ビジネスロジック |
| features/ | 他featureへの直接依存 |
| api/ | ビジネスロジック（src/lib/servicesに委譲する）|
| lib/services/ | HTTPの詳細・フレームワークの型 |

---

## 4. ⚠️ 禁止事項（最重要）

### コードの配置
- 最上位コンポーネント（App.tsx等）に機能固有ロジックを書かない
- `utils/` に状態を持つコードを置かない
- `components/` から直接APIを叩かない

### 変更の範囲
- 指示されていないファイルを「ついでに」リファクタリングしない
- 既存テストが通る状態を崩さない
- `package.json` のバージョンを勝手に上げない

### セキュリティ
- APIキー・シークレットをコードにハードコードしない（環境変数を使う）
- SQLを文字列結合で組み立てない

---
## 5. 環境変数（TODO: 確定したら記入）
- Supabase関連（SUPABASE_URL / ANON_KEY / SERVICE_ROLE_KEY ）
- LLM_API_KEY

---

## 6. データフロー
<!-- 主要な処理の流れを矢印で書く。複雑なシステムほど効果大 -->
主要な処理の流れを以下のように矢印で書く。
```
[ユーザー操作]
    ↓
[features/] 入力バリデーション
    ↓
[api/xxx.ts] Zodでバリデーション → lib/servicesに委譲
    ↓
[lib/services/] ビジネスロジック・外部API呼び出し（Supabase, LLM等）
    ↓
[api/xxx.ts] JSONで返す

```

---

## 7. 意思決定ログ
<!-- 「なぜそう作ったか」を残す。理由を知らずにClaudeがリファクタリングして壊すことを防ぐ -->
「なぜ、このような設計にしたのか」という変更ログを残していく。
また、現状をわからずにClaudeが勝手にリファクタリングして破壊することを防ぐ。
### ADR-001: <!-- タイトル -->
- **決定**: 
- **理由**: 
- **トレードオフ**: 

---

*最終更新: 2026-06-03*
